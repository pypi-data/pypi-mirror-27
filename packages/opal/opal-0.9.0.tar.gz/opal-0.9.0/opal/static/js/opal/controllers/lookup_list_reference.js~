controllers.controller('LookupListReferenceCtrl', function($scope, $modalInstance, lookuplist){
    $scope.lookuplist = lookuplist;

  $scope.cancel = function () {
    $modalInstance.dismiss('cancel');
  };

});
