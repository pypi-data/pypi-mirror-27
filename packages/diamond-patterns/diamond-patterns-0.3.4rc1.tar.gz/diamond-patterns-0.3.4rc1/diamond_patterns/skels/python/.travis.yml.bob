language: python
cache: pip
python:
  - 2.7
  - 3.5
install:
  - make dev install
script:
  - nosetests -w {{{ application.module }}} -c etc/tests.cfg --with-coverage --cover-package={{{ application.module }}}
after_success:
  - coveralls --rcfile=.coveragerc
