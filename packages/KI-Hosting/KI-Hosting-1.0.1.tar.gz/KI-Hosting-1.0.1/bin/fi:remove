#!/usr/bin/env python

from optparse import OptionParser
from pymongo import MongoClient

parser = OptionParser()

if __name__ == "__main__":
    (options, args) = parser.parse_args()
    slug = args[0]
    client = MongoClient()
    db = client.fiches
    db.clubs.delete_one({ '_id': slug })
