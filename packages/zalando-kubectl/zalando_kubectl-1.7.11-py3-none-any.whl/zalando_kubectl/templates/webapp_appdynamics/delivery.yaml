deployment_steps:
- id: deployment
  desc: Deploying nginx-hello-world to $cluster_id
  target: $cluster_id
  process: microservice_standard_deployment
  config:
    apply_permanent_resources:
      env:
        APPLICATION: nginx-hello-world
        VERSION: "0.1"
        REPLICAS: "3"
